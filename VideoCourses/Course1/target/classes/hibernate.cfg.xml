<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Параметры соединения с базой данных -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/hibernate</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">1854</property>

        <!-- Выводить исполняемые фреймвоком SQL запросы(удобно для отладки и понимания что происходит) -->
        <property name="show_sql">true</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Параметр задает необходимость создания/удаления таблиц и колонок в БД в случае их отсутствия
         В данном конфиге почему то не работает-->
        <property name="hibernate.hbm2ddl.auto">create-drop</property>

        <!-- Файл маппинга(описывает связь между классом и таблицей. Может быть несколько) -->
        <mapping resource="userM.cfg.xml"/>
    </session-factory>
</hibernate-configuration>


